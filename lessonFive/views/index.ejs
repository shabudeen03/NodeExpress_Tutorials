<!-- views/index.ejs -->
<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <hr />
  <br />
  <form method="GET" action="/search">
    <label for="search">Search for user(s) by name or email:</label>
    <input type="text" id="search" name="search" placeholder="name or email ..." required />
    <button type="submit">Search</button>
  </form>
  <ul>
    <% if (locals.users) {%>
      <% users.forEach(user => { %>
        <%- include("partials/user", { user: user}) %>
        <!-- <li>
          <h3>ID: <%= user.id %>, Name: <%= user.firstName %> <%= user.lastName %></h3>
          <div>
            Email: <%= user.email %>
            <% if(user.age) { %>
                <%= ", Age: " + user.age %></div>
            <% } %>
          <% if(user.bio) { %> <p>
            <%= user.bio %>
            </p> <% } %>
          <a href="/<%= user.id %>/update">Update</a>
          <form action="/<%= user.id %>/delete" method="POST" style="display:inline;">
            <button type="submit" onclick="return confirm('Are you sure you want to delete this user?')">Delete</button>
          </form>
        </li> -->
      <% }); %>
    <% } %>
  </ul>
  <a href="/create">Create a user</a>
</body>
</html>